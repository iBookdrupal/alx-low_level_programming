#include <stdio.h>
#include "main.h"
/**
* set_string -> set string to char.
* @s: double pointer
* @to: single pointer
* Return: 0 always successful
*/

void set_string(char **s, char *to)
{
	*s = to;
}
