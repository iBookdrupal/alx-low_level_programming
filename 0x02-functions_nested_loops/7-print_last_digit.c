#include "main.h"
/**
* print_last_digit -> prints the last digit
* @last: returns the last digit
* Return: 0 always
*
*/

int print_last_digit(int last)
{
	l = last % 10
	_putchar(l);
	return (l);
}
